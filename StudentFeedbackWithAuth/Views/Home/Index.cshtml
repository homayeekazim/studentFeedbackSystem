@model StudentFeedbackWithAuth.ViewModels.QuoteOfTheDayViewModel
@{
    ViewData["Title"] = "Home Page";
}

<div class="text-center">
    <h5 class="display-4 mb-5">Welcome Students!</h5>
    @await Html.PartialAsync("_Quote", Model)
</div>
